name: Update Football Content

permissions:
  contents: write

on:
  schedule:
    - cron: '0 20 * * *'   # каждый день в 20:00 UTC
  workflow_dispatch:

jobs:
  update-content:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Create .env file
        run: |
          # для скрипта fetch-scores.js (APIfootball.com)
          echo "APIFOOTBALL_API_KEY=${{ secrets.APIFOOTBALL_API_KEY }}" >> .env

          # для скрипта fetch-news.js
          echo "OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}"              >> .env
          echo "VITE_NEWS_SOURCE_URL=${{ secrets.VITE_NEWS_SOURCE_URL }}" >> .env

          # лимиты для OpenAI
          echo "DAILY_LIMIT=5"                                           >> .env
          echo "THROTTLE_MS=120000"                                      >> .env

      - name: Fetch and process football news
        run: node scripts/fetch-news.js

      - name: Fetch football scores
        run: node scripts/fetch-scores.js

      - name: Commit and push if content changed
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name  "github-actions[bot]"

          # stage изменения в public/data
          git add public/data/

          # если есть staged изменения — пробуем забрать remote, чтобы не было конфликтов
          if ! git diff --quiet --cached; then
            echo "🎬 Changes detected, pulling remote and rebasing with autostash…"
            git pull --rebase --autostash origin main

            echo "💾 Committing updated data…"
            git commit -m "Update football content [skip ci]"

            echo "🚀 Pushing to origin/main…"
            git push
          else
            echo "✅ No changes to commit"
          fi
